<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Net192168.Client.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="32,28,32,24" Spacing="20">
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Settings" />

        <StackPanel Spacing="8">
            <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Server" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="The address of the server that introduces you to your friends. Leave it alone unless you were given another one."
                TextWrapping="Wrap" />

            <TextBox
                MaxWidth="420"
                HorizontalAlignment="Left"
                PlaceholderText="https://api.192168.lol"
                Text="{x:Bind ViewModel.ServerUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Command="{x:Bind ViewModel.TestCommand}" Content="Test" />
                <Button
                    Command="{x:Bind ViewModel.SaveCommand}"
                    Content="Save"
                    Style="{StaticResource AccentButtonStyle}" />
                <ProgressRing
                    Width="20"
                    Height="20"
                    IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />
            </StackPanel>

            <InfoBar
                MaxWidth="420"
                HorizontalAlignment="Left"
                IsClosable="False"
                IsOpen="{x:Bind ViewModel.Result, Mode=OneWay, Converter={StaticResource NotNullToBoolean}}"
                Message="{x:Bind ViewModel.Result, Mode=OneWay}" />
        </StackPanel>
    </StackPanel>
</Page>
