<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Net192168.Client.Views.GroupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    KeyDown="OnKeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Back and a title, the same shape on every screen that is not home.  -->
        <Grid
            Grid.Row="0"
            Margin="8,0,16,8"
            ColumnSpacing="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Width="32"
                Height="32"
                Padding="0"
                AutomationProperties.Name="Back"
                Background="Transparent"
                BorderThickness="0"
                Click="OnBack"
                ToolTipService.ToolTip="Back">
                <FontIcon FontSize="14" Glyph="&#xE72B;" />
            </Button>

            <TextBlock
                Grid.Column="1"
                VerticalAlignment="Center"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{x:Bind ViewModel.Title}" />
        </Grid>

        <StackPanel
            Grid.Row="1"
            Margin="16,4,16,0"
            Spacing="14">
            <TextBox
                x:Name="NameBox"
                Header="Group name"
                PlaceholderText="Friday Night"
                Text="{x:Bind ViewModel.GroupName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <!--
                PasswordBox does not push its value on every keystroke the way a
                TextBox does, so the change event carries it across.
            -->
            <PasswordBox
                x:Name="PasswordInput"
                Header="Password"
                PasswordChanged="OnPasswordChanged" />

            <TextBox
                Header="Your nickname"
                Text="{x:Bind ViewModel.Nickname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.Hint}"
                TextWrapping="Wrap" />
        </StackPanel>

        <Grid
            Grid.Row="2"
            Padding="16,12"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Failing here keeps the screen and the typing, so this is where it says why.  -->
            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.Error, Mode=OneWay}"
                TextWrapping="Wrap" />

            <Button
                Grid.Column="1"
                Click="OnSubmit"
                Content="{x:Bind ViewModel.SubmitLabel}"
                IsEnabled="{x:Bind ViewModel.CanSubmit, Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}" />
        </Grid>
    </Grid>
</Page>
